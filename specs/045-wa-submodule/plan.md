# Plan 045 — WA submodule

**Branch**: `opencode/spec-045-wa-submodule`

## Summary

Sustituir clone local por submodule `@ v1.33.5`; scripts y política fuera del vendor.

## Implementation

1. `git submodule add` + checkout `v1.33.5`
2. `scripts/wa/upload-starter-to-map-storage.sh`
3. Docs + INDEX
